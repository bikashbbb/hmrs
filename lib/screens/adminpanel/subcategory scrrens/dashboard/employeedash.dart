import 'package:flutter/material.dart';

class Employye_interface extends StatelessWidget {
  const Employye_interface({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
